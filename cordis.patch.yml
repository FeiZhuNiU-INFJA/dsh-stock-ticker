# dsh-stock-ticker bundle patch: insert the plugin as a single loader entry.
# The host entry registers the same-origin `/dsh-stock-ticker/quotes` route;
# the client bundle (lib/client.js) renders the floating window and polls it.
- insert:
    - id: stock-ticker
      name: dsh-stock-ticker
